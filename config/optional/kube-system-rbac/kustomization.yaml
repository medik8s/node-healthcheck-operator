apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# RBAC for reading the client CA from kube-system/extension-apiserver-authentication
# Used by the MetricsClientCA controller for Platform Prometheus TLS authentication
#
# Note: Do NOT include patches/common here - it would overwrite namespace: kube-system

resources:
- kube_system_role.yaml
- kube_system_role_binding.yaml
